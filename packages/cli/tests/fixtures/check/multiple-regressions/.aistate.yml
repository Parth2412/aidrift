version: "1"
name: check-fixture
artifacts:
  models:
    primary:
      type: model
      provider: custom
      model: gpt-4o-2024-08-06
eval:
  suite: ./evals
storage:
  backend: local
  path: ./.aidrift/snapshots
