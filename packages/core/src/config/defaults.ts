import type { ResolvedAIDriftConfig } from "./types.js";

export const DEFAULT_AIDRIFT_CONFIG: ResolvedAIDriftConfig = {
  configPath: undefined,
  format: "text",
  logLevel: "warn",
  color: true,
};
