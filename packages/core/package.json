{
  "name": "@aidrift/core",
  "version": "0.0.0",
  "description": "Core engines for AIDRIFT behavioral state management.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist *.tsbuildinfo",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/diff": "^8.0.0",
    "typescript": "^5.8.3",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "diff": "^9.0.0",
    "glob": "^10.5.0",
    "yaml": "^2.8.3"
  }
}
